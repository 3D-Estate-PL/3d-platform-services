# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

parameters:
- name: clean
  displayName: Checkout clean
  type: boolean
  default: true


trigger:
  branches:  
    include:      
    - develop
    - main
    - feature/*
  paths:
    include:
    - src/BuildingBlocks/**    
    - src/Services/InteriorDesigners/**
    - deploy/interior-designers-azure-pipelines.yml
    - deploy/containerapps/modules/apps/interior-designers-api.bicep


pool:
  name: Default

variables:
  isMain: $[eq(variables['Build.SourceBranch'], 'refs/heads/main')]
  applicationName: platform
  azureSubscription: platform-services-prod-asc
  serviceName: interior-designers-api
  repositoryName: interior-designers.api

stages:
- template: templates/container-app-stages.yml  # Template reference
  parameters:
    serviceName: $(serviceName)
